# Comma-separated browser origins permitted to upgrade /ws/game.
# Add the deployed frontend origin in production; do not use a global wildcard.
GAME_WEBSOCKET_ALLOWED_ORIGIN_PATTERNS=http://localhost:*,http://127.0.0.1:*
